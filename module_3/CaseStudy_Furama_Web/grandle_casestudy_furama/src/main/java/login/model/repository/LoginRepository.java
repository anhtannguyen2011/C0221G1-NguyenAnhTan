package login.model.repository;

public class LoginRepository {
    BaseRepository baseRepository = new BaseRepository();
   private static final String SELECT_ACCOUNT = "select * form user_view where ";
}
